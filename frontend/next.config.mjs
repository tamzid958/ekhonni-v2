/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['static.nike.com', 'images.unsplash.com'], // Allow images from static.nike.com
  },
};

export default nextConfig;
